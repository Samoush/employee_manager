<%= stylesheet_link_tag "style" %>

<head>
  <%= render 'navigation' %>

  <div class='title_box'>
    <h1 id='title'>Anlegen eines Auftrags</h1>
  </div>
</head>

<div class='formular'>
  <%= simple_form_for @order do |f| %>
    <%= render 'form', f:f %>

    <div class='row'>
      <%= f.button :submit, 'Auftrag Erstellen' %>
    </div>
  <% end %>
</div>
